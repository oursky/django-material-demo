from django.forms import Widget

class Switch(Widget):
    # django-material will automatically find the template in "material/fields/{module}_{class}.html",
    # so the following line is just for reference
    # template_name = "material/fields/component_switch.html"
    pass
